/**
 * Kept as a placeholder to avoid breaking the build.
 * The Better Auth–based private data pattern has been removed.
 * All data access is now gated by Phantom session validation in auth.ts.
 */

export {};
