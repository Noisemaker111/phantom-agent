/**
 * This route is not used in the Chrome extension build.
 * The conversation UI is provided by PhantomConversation (sidepanel.tsx / popup.tsx).
 * Kept as an empty stub to avoid breaking the file structure.
 */

export {};
