# Required — your Phantom Portal application ID
PHANTOM_APP_ID=your-phantom-app-id-here

# Required — shared secret between this server and your Convex deployment.
# Generate with: openssl rand -hex 32
PHANTOM_MCP_SHARED_SECRET=generate-a-random-secret-here

# Optional — HTTP port (Railway sets this automatically)
PORT=3000

# Optional — enable debug logging
# PHANTOM_MCP_DEBUG=1
